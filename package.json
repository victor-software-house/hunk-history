{
  "name": "hunk-history",
  "version": "0.0.4",
  "description": "Review a commit series or inclusive range in Hunk with commit messages and full timestamps.",
  "type": "module",
  "license": "MIT",
  "hunk": {
    "apiVersion": 16
  },
  "devDependencies": {
    "@opentui/core": "^0.5.10",
    "@opentui/react": "^0.5.10",
    "@types/node": "^26.4.1",
    "@types/react": "^19.2.18",
    "hunkdiff": "^0.21.1",
    "react": "^19.2.8",
    "typescript": "^7.0.2"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.ts && bun test ./test/pane.test.tsx"
  }
}
